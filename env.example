OPENAI_API_KEY=sk-your-key-here

# Optional overrides — defaults are already set in the Dockerfile
# DB_PATH=/app/data/session_history.db
# FALLBACK_DIR=/app/data/log/fallback.db
# CREATE_FAISS_INDEX=1   # force a rebuild of the FAISS index on next run
